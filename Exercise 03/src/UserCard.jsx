const Greeting = ({name,role}) => {
  // console.log(role)
    return(
        <div>
            <h1>My name is {name}</h1>
            <p>My role is {role}</p>
        </div>
    )
}

export default Greeting;
